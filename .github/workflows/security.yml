name: security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '0 17 * * 0'

permissions:
  contents: read
  security-events: write

jobs:
  codeql:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        language: [python, javascript-typescript]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: github/codeql-action/init@a2983b8bed1923f44751c5c43237f479442827b3 # v3
        with:
          language: ${{ matrix.language }}
          queries: security-extended
      - uses: github/codeql-action/analyze@a2983b8bed1923f44751c5c43237f479442827b3 # v3

  semgrep:
    runs-on: ubuntu-latest
    container: semgrep/semgrep
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: semgrep ci --config p/python --config p/react --config p/secrets --config p/dockerfile
        env:
          SEMGREP_RULES: p/default

  python-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'
      # The `sarif` extra supplies the -f sarif formatter. Without it bandit
      # exits 2 ("invalid choice: 'sarif'"), continue-on-error hides that, no
      # bandit.sarif is written, and the upload step below fails the job — so
      # this reported red for a missing formatter while pip-audit was green
      # and bandit had never actually run.
      - run: pip install 'bandit[toml,sarif]' pip-audit
      - run: bandit -r backend/app -ll -f sarif -o bandit.sarif
        continue-on-error: true   # findings must not fail the job; the SARIF is the report
      - run: pip-audit -r backend/requirements.txt
      - uses: github/codeql-action/upload-sarif@a2983b8bed1923f44751c5c43237f479442827b3 # v3
        if: always()
        with:
          sarif_file: bandit.sarif

  node-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci --prefix frontend
      # --omit=dev: the frontend ships as static files built by Vite, so only
      # runtime dependencies reach a user. The dev toolchain (vite, vitest,
      # esbuild) carries its own advisories — real, but they describe a dev
      # server that is never exposed in a deployment, and clearing them means
      # a vite 5->8 / vitest 2->4 major bump. Those are tracked separately;
      # gating releases on them would have blocked shipping for a risk that
      # is not in the artifact. Runtime dependencies are still gated at high.
      - run: npm audit --prefix frontend --audit-level=high --omit=dev

  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7 # v2

  container:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: docker build -f Dockerfile.prod -t reqmesh:ci .
      # Pinned to a release, not to a `master` commit as it was: an unversioned
      # pin cannot be reasoned about or upgraded deliberately.
      - uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        env:
          # Pull the vulnerability DB from the public GCR mirror rather than
          # ghcr.io. Anonymous ghcr pulls from Actions runners are rate-limited,
          # and this step was dying in ~15s — far too fast to have scanned
          # anything, on an image that scans clean with 0 findings. It read as
          # "a CVE is blocking the release" for days when nothing was.
          TRIVY_DB_REPOSITORY: mirror.gcr.io/aquasec/trivy-db:2
          TRIVY_JAVA_DB_REPOSITORY: mirror.gcr.io/aquasec/trivy-java-db:1
        with:
          image-ref: reqmesh:ci
          severity: HIGH,CRITICAL
          # ignore-unfixed: a Debian base image always carries some HIGH CVEs
          # that upstream has not patched. Failing on those makes the gate
          # unsatisfiable — there is no action that clears it — so it stops
          # being read. Fail only on CVEs that a rebuild or a version bump can
          # actually fix; the Dockerfile now runs apt-get upgrade so those get
          # picked up. Unfixed CVEs are still reported in the SARIF upload
          # below, they just don't block.
          ignore-unfixed: true
          exit-code: '1'
          format: sarif
          output: trivy.sarif
      - uses: github/codeql-action/upload-sarif@a2983b8bed1923f44751c5c43237f479442827b3 # v3
        if: always()
        with:
          sarif_file: trivy.sarif
