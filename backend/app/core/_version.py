# Generated file — do not edit by hand.
# The authoritative version lives in the repo-root VERSION file; scripts/set_version.py
# regenerates this module so the backend has a version even when the source tree is
# repackaged (e.g. copied into /app inside the Docker image, where VERSION is not
# on a predictable relative path).
__version__ = "0.0.4"
