# reqmesh environment — generated by install wizard
# Do not edit by hand; re-run ./install.sh to reconfigure.

RT_PROFILE=${PROFILE}
RT_SECRET=${RT_SECRET}
RT_ADMIN_PASSWORD=${ADMIN_PASSWORD}
RT_BASE_URL=${BASE_URL}
RT_HOST=${HOST}
RT_PORT=${PORT}
RT_STATIC_DIR=${STATIC_DIR}
RT_DATA_ROOT=${DATA_ROOT}
RT_SEED_DEMO=${SEED_DEMO}
RT_OFFLINE_MODE=${OFFLINE_MODE}
RT_REQUIRE_AUTH=${REQUIRE_AUTH}
RT_ALLOW_SELF_REGISTRATION=${SELF_REG}
RT_COOKIE_SECURE=${COOKIE_SECURE}
RT_REQUIRE_EMAIL_VERIFICATION=${REQUIRE_EMAIL_VERIFICATION}
RT_ALLOWED_HOSTS=${ALLOWED_HOSTS}
RT_GIT_AUTOCOMMIT=${GIT_AUTOCOMMIT}
RT_GIT_REMOTE_URL=${GIT_REMOTE_URL}
RT_GIT_PUSH_ON_COMMIT=${GIT_PUSH_ON_COMMIT}
RT_GIT_PUSH_INTERVAL_MINUTES=${GIT_PUSH_INTERVAL_MINUTES}
RT_CORS_ORIGINS=${CORS_ORIGINS}
RT_UPDATE_CONTROL_DIR=${UPDATE_CONTROL_DIR}
RT_PROXY_TRUSTED_CIDR=${PROXY_TRUSTED_CIDR}

# SMTP (empty = disabled)
RT_SMTP_HOST=${SMTP_HOST}
RT_SMTP_PORT=${SMTP_PORT}
RT_SMTP_USERNAME=${SMTP_USERNAME}
RT_SMTP_PASSWORD=${SMTP_PASSWORD}
RT_SMTP_FROM=${SMTP_FROM}
RT_SMTP_USE_TLS=${SMTP_USE_TLS}

# Branding
RT_REPORT_COMPANY_NAME=${REPORT_COMPANY_NAME}
RT_REPORT_DOCUMENT_TITLE=${REPORT_DOCUMENT_TITLE}
RT_REPORT_LOGO_URL=${REPORT_LOGO_URL}

# Git identity (used for autocommit author)
GIT_USER_NAME=${GIT_USER_NAME}
GIT_USER_EMAIL=${GIT_USER_EMAIL}
